## 🚀 Description

<!-- 무엇을 왜 바꿨는지. 리뷰어가 코드를 열기 전에 이해할 수 있게. -->

## 🔗 관련 이슈

<!--
이 PR 이 머지되면 이슈가 자동으로 닫히게 하려면 아래 키워드와 이슈 번호를 적는다.

  Closes #12

- 쓸 수 있는 키워드: Closes / Fixes / Resolves (대소문자 무관)
- 이슈가 여러 개면 번호마다 키워드를 붙인다.  Closes #12, closes #13
  ("Closes #12, #13" 은 #12 만 닫힌다)
- 기본 브랜치(main)로 머지될 때만 자동으로 닫힌다. 다른 브랜치로 보내면 안 닫힌다.
- 이슈의 일부만 처리했으면 닫지 말고 "관련 #12" 로만 적는다.
-->

Closes #

## ✅ 확인한 것

<!--
젯슨 없이 돌릴 수 있는 것부터:
  cd vlm && python -m unittest discover
  cd mvp && python -m unittest discover -s tests -t .
젯슨 실기에서 확인했으면 어떤 구성(VLM config / TTS 엔진)으로 했는지 같이 적는다.
-->

- [ ] `vlm` 단위 테스트
- [ ] `mvp` 단위 테스트
- [ ] 젯슨 실기 확인 (구성: )

## 📸 Screenshot

<!-- 화면이나 대시보드가 바뀌면 before / after. 없으면 "해당 없음". -->

## 📢 Notes

<!-- 리뷰어가 알아야 할 것. 미처리로 남긴 것, 메모리 영향, 확인 부탁할 것. -->
